# cookbook

A lightweight Python cookbook package with a tiny CLI for searching recipes.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e '.[dev]'
pytest
```

## CLI usage

```bash
cookbook avocado
cookbook --tag quick
```
